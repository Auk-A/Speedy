package avans.deeltijd.speedy.domain;

public enum CustomResponse {
    PLATE_NOT_FOUND,
    CAR_ALREADY_EXISTS,
    NEW_CAR_ADDED,
    USER_ALREADY_EXISTS,
    NEW_USER_ADDED,
    INCORRECT_DATE_FORMAT,
    TRIP_STARTED,
    RESERVATION_CREATED,
    RESERVATION_FAILED,
}
